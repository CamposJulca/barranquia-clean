# Bloque H — Eliminación definitiva del seed sintético

Fecha: 2026-05-12 16:06 UTC
Branch: main
Commit: `3dbcf05`
Decisión motivadora: **El proyecto opera exclusivamente sobre el subdata real del ERP de J.O.Z. (SuperEfectivo). No hay datos sintéticos en ningún entorno.**

## Tareas completadas

- [x] **H1** — Inspección de dependencias (resultado abajo).
- [x] **H2** — `git rm joz/backend/joz/management/commands/seed_joz.py`.
- [x] **H3** — `git stash drop stash@{0}` (el que tenía solo seed_joz.py reescrito).
- [x] **H4a** — `views.py:etl_run` actualizado a mensaje neutral.
- [x] **H4b** — 5 documentos actualizados (incluida nueva §11.5 del doc v2.1).
- [x] **H5** — `manage.py check` 0 issues.

## H1 — Inspección previa

Buscado en docs, código, infra (Dockerfile, entrypoint, compose, Makefile) y workflows. Resultados:

| Ubicación | Tipo | Acción |
|-----------|------|--------|
| `Makefile` raíz | Infra | Sin referencias. |
| `joz/infra/Dockerfile.backend`, `joz/infra/entrypoint.sh` | Infra | Sin referencias. |
| `joz/docker-compose.yml`, `shared/docker-compose.yml` | Infra | Sin referencias. |
| `.github/workflows/` | CI | No existe esa carpeta. |
| `joz/backend/joz/fixtures/` | Fixtures | No existe. |
| `joz/backend/joz/views.py:919-924` | **Código runtime** | Sí — el mensaje del endpoint `etl_run` sugería al usuario correr `seed_joz`. Resuelto en H4a tras decisión del responsable. |
| `correcciones/DOCUMENTO_TECNICO_FUNCIONAL_JOZ.md` (v2.1) | Doc oficial | 2 refs (línea 182 árbol, 644 comando). Resuelto. |
| `docs/DESPLIEGUE_LOCAL.md` | Doc despliegue | 4 refs (174, 184, 376, 423). Resuelto. |
| `joz/docs/PLAN_DE_TRABAJO.md:124` | Doc plan | 1 ref justificativa del seed. Resuelto. |
| `joz/docs/DOCUMENTO_TECNICO_FUNCIONAL_JOZ.md` | Doc viejo del módulo | 2 refs (148 árbol, 541 comando). **Archivo untracked en git** — la edición se aplicó en disco pero no entra al commit. |

No había referencias en infra de despliegue. Sí en código runtime (views.py) — se consultó y se decidió reemplazar por mensaje neutral.

## H2 — Archivo eliminado

```
D  joz/backend/joz/management/commands/seed_joz.py   (−116 líneas)
```

El archivo trackeado en main era la versión de 244 transacciones reales hardcoded del PDF. No queda fixtures ni imports residuales que dependan del comando.

## H3 — Stash

```
ANTES:
  stash@{0}: pending: seed_joz.py revision con Cristhiam
  stash@{1}: guardando cambios locales

DESPUÉS:
  stash@{0}: guardando cambios locales   (preexistente, no nuestro)
```

`git stash drop` ejecutado sobre el stash del seed reescrito (sha `1464cdc9...`). El stash@{1} histórico se preservó.

La consulta a Cristhiam queda **superada por la decisión de principio**: aunque su versión sintética tenía valor para demos, el principio de datos reales la deja fuera del alcance del proyecto. No tiene sentido preguntarle por una refactorización que va a terminar en "no entra al repo igual".

## H4a — Cambio en `views.py:etl_run`

Antes:

```python
'mensaje': 'ETL deshabilitado: la conexión a la API externa está suspendida. '
           'Use "python manage.py seed_joz" para cargar datos de prueba.',
```

Después:

```python
'mensaje': 'ETL deshabilitado: la conexión a la API externa está suspendida. '
           'Contacte al administrador para restaurar datos desde el subdata del ERP.',
```

Docstring de la función actualizado en línea con el principio. El cliente que pega el botón "Lanzar ETL manual" en el dashboard ahora ve un mensaje que apunta al canal correcto (administrador / dump / coordinación) en lugar de a un comando inexistente.

## H4b — Documentos actualizados

### `correcciones/DOCUMENTO_TECNICO_FUNCIONAL_JOZ.md` (v2.1, oficial)

- §3.4 árbol de directorios: `seed_joz.py` retirado del listado de management commands.
- §11.2 comandos de gestión: bloque de "Sembrar datos de demostración" eliminado.
- **NUEVA §11.5 — Principio operativo: datos reales.** Sección corta que documenta la decisión: no hay datos sintéticos, los entornos locales se pueblan por dump o ETL manual, y `seed_joz` fue retirado en 2026-05-12.

### `docs/DESPLIEGUE_LOCAL.md`

- Sección "Joz — Transacciones SuperEfectivo" reescrita: ya no muestra el comando seed; explica el principio y las dos vías para poblar un entorno local (dump del subdata o ETL manual).
- Árbol de directorios `joz/backend/joz/management/commands/` actualizado: ahora lista `detectar_anomalias.py` y `calcular_riesgos.py` (los dos comandos que sí existen).
- Sección "El seed falla con 'already exists'": el bloque de comandos quita la línea de JOZ; queda nota dirigiendo a §11.5 del v2.1.

### `joz/docs/PLAN_DE_TRABAJO.md`

- Nota del párrafo 124 reformulada: ya no dice "el equipo trabaja con el seed de 244 transacciones reales"; ahora dice "el equipo trabaja sobre dumps recientes del subdata real del ERP" con referencia explícita al principio §11.5.

### `joz/docs/DOCUMENTO_TECNICO_FUNCIONAL_JOZ.md` (untracked)

Encontrado durante el grep, descubierto untracked. Apliqué la misma edición que al doc oficial (quitar `seed_joz.py` del árbol, quitar comando de seed) **pero el archivo permanece untracked** — no entra al commit H. Si en el futuro alguien decide versionarlo, la edición queda en disco y queda alineada con el principio.

> Observación: prácticamente todos los `.md` de `joz/docs/` están untracked. El único tracked es `PLAN_DE_TRABAJO.md`. Eso sugiere que toda esa carpeta es trabajo paralelo no commiteado. Conviene revisar en un bloque futuro si esos docs deben versionarse, descartarse o dejarse como artefactos locales.

## H5 — Validación

| Check | Resultado |
|-------|-----------|
| `python manage.py check` en `barranquia_joz_backend` | ✅ System check identified no issues (0 silenced). |
| `python manage.py help` lista `seed_joz` | ⚠️ Sí — pero porque el contenedor corre la imagen pre-H (sin rebuild). Es coherente con la nota G6: el rebuild se aplaza hasta reactivar el ETL. Cuando se rebuilde, `seed_joz` desaparecerá del help. |
| `grep -rn "seed_joz"` en repo (sin reportes ni archivo) | ✅ Solo queda la línea en §11.5 del v2.1 que documenta el retiro — intencional. |

## H6 — Commits

Dos commits, siguiendo el patrón del Bloque G:

- `3dbcf05` — `[JOZ-correcciones] H: eliminar seed sintético — principio de datos reales`. Código + docs trackeados. 5 archivos, +24/−135.
- (este reporte) — Va en commit separado con `[JOZ-correcciones] docs: reporte bloque H`.

## Estado final

✅ **Completo**. El seed sintético dejó de existir como concepto en el repo:

- Archivo eliminado.
- Mensajes de API actualizados.
- Documentación trackeada limpia.
- Stash droppeado.
- Principio documentado en §11.5 del doc v2.1.

Pendiente derivado (para una conversación posterior): los docs untracked en `joz/docs/` son un punto ciego que conviene revisar en un bloque futuro.
